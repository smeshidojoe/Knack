# Knack — runtime dependencies.
#   pip install -r requirements.txt

# GUI.
PySide6==6.11.1

# Обложки медиа, миниатюры полки.
pillow==12.1.0

# Windows Media Session (SMTC): что играет, обложка, перемотка.
# Берём проекции pywinrt, а не `winsdk`: у winsdk под Python 3.13 нет готовых
# колёс, только исходники — сборка потребовала бы MSVC.
winrt-Windows.Media.Control==3.2.1
winrt-Windows.Storage.Streams==3.2.1
winrt-Windows.Foundation==3.2.1
# Нужен для GetSessions: без него виден только один текущий источник звука.
winrt-Windows.Foundation.Collections==3.2.1


# --- Позже (вкладка «Переводчик») ------------------------------------------ #
# argostranslate      — офлайн-перевод
# requests            — DeepL API

# --- Build only ------------------------------------------------------------ #
# pyinstaller
